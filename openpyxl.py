### ChatGPT answer )
''' В библиотеке openpyxl для работы с ячейками (класс Cell) существует множество методов и атрибутов помимо cell.value. Вот некоторые из них:

Основные атрибуты и методы:
cell.coordinate: Возвращает координаты ячейки в виде строки, например, 'A1'.

cell.row: Возвращает номер строки, в которой находится ячейка.

cell.column: Возвращает номер столбца (как число), в котором находится ячейка.

cell.column_letter: Возвращает букву столбца, в котором находится ячейка, например, 'A'.

cell.font: Возвращает или устанавливает объект Font, определяющий шрифт ячейки.

cell.fill: Возвращает или устанавливает объект PatternFill, который определяет заливку ячейки.

cell.border: Возвращает или устанавливает объект Border, который определяет границы ячейки.

cell.alignment: Возвращает или устанавливает объект Alignment, который определяет выравнивание текста в ячейке.

cell.number_format: Возвращает или устанавливает формат числа в ячейке, например, '0.00%' или '$#,##0.00'.

cell.protection: Возвращает или устанавливает объект Protection, который определяет защиту ячейки, например, блокировку.

cell.comment: Возвращает или устанавливает объект Comment, который определяет комментарий в ячейке.

cell.data_type: Возвращает тип данных ячейки (например, 's' для строки, 'n' для числа).

cell.hyperlink: Возвращает или устанавливает гиперссылку в ячейке.

cell.is_date: Возвращает True, если значение ячейки является датой.

cell.internal_value: Возвращает внутреннее значение ячейки, как оно хранится в файле Excel.

cell.offset(row, column): Возвращает ячейку, смещенную относительно текущей на указанное количество строк и столбцов.'''

from openpyxl import Workbook

wb = Workbook()
ws = wb.active
cell = ws['A1']

# Установка значения
cell.value = "Hello, World!"

# Применение шрифта
from openpyxl.styles import Font
cell.font = Font(bold=True, italic=True)

# Применение заливки
from openpyxl.styles import PatternFill
cell.fill = PatternFill("solid", fgColor="DDDDDD")

# Получение координат ячейки
print(cell.coordinate)  # 'A1'

